# Cat Actions

Each action folder contains the source video, extracted frames, preview media, and optional notes for one cat animation.

Use these folders while converting the user's real cat into MomoDesk animation assets. Private raw source media can be stored here during local work, but large generated files should be committed only when they are final and intentionally part of the app.

For the complete directory and format contract, see `docs/action-assets.md`.
