# Atlas Output

Put packed production sprite sheets and atlas metadata here.

Expected final files:

- `cat_spritesheet.png` or `cat_spritesheet.webp`
- `cat.atlas.json`

Intermediate action frames should stay under `assets/pets/default/actions/<action>/frames/` until they are packed.
